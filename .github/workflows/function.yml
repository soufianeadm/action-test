name: build
on: push
jobs:
  function:
    runs-on: ubuntu-latest
    steps:
      - name: dump
        run: |
          echo ${{ contain('hello','ll')}}
          echo ${{ startsWith('hello','he') }}